{
  "installCommand": "pnpm install --frozen-lockfile --prod=false",
  "buildCommand": "turbo build --filter=@atrium/ui --filter=web",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
